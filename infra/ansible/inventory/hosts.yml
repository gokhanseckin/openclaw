all:
  children:
    openclaw:
      hosts:
        tbc-prod:
          ansible_host: "178.104.45.149"
          ansible_user: root
          ansible_ssh_private_key_file: "~/.ssh/id_ed25519"
          # Pin to a release tag (set to "main" only intentionally).
          openclaw_version: "v2026.4.26"
          # Internal port. Bound to 127.0.0.1 only — never opened in the firewall.
          # Reach it from your laptop via:
          #   ssh -L 18789:127.0.0.1:18789 root@178.104.45.149
          # Then http://localhost:18789/ in your browser.
          openclaw_port: 18789
